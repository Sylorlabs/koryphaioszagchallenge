// Configuration types for Koryphaios
// Re-export from shared to ensure consistency

export type {
  KoryphaiosConfig,
  ProviderConfig,
  MCPServerConfig,
  SafetyLimits,
  WorkspaceConfig,
  ServerConfig,
} from '@koryphaios/shared';
